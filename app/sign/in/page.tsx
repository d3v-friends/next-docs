import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "d3v-friends - sign in",
    description: "sign in",
};

export default function Page() {
    return (
        <>
            <div>SignIn</div>
        </>
    );
}
